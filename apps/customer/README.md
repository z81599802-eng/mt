# Mubarak Tabarak Customer PWA

- Next.js 14 App Router + Tailwind + React Query + NextAuth
- OTP login via custom credentials provider calling the API.
- PWA manifest + minimal service worker scaffold.

## Development

```bash
cd apps/customer
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` to the deployed API origin.
